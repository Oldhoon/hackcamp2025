import { Card } from "./ui/card";

export const PostureMonitor = () => {
  return (
    <Card className="card column gap">
      <h3>Posture Monitor</h3>
      <p className="muted small">Placeholder — connect to backend posture API.</p>
    </Card>
  );
};
